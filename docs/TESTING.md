# 测试文档

## 测试框架

- **Jest**：JavaScript 测试运行器
- **Supertest**：HTTP 断言库，用于测试 Express API

## 运行测试

```bash
# 运行全部测试
npm test

# 带覆盖率报告
npm test -- --coverage

# 调试特定测试文件
npx jest backend/tests/photos.test.js
```

## 测试文件结构

```
backend/tests/
├── auth.test.js    # 认证接口测试
├── photos.test.js  # 照片接口测试
└── albums.test.js  # 相册接口测试
```

## 测试设计原则

### 数据隔离与清理

每个测试文件在 `afterAll` 中负责清理自身产生的测试数据：
- 删除测试创建的照片和相册数据库记录
- 删除测试上传的物理文件（`storage/` 目录下的图片）
- 关闭 SQLite 数据库连接，防止 Jest 进程挂起

### 登录态复用

每个测试文件在 `beforeAll` 中完成一次登录，获取 `set-cookie`，后续用例复用该 Cookie，减少重复登录开销。

### 等待数据库就绪

由于 `initializeDatabase()` 是异步操作，所有测试文件都通过 `await app.ready` 确保数据库已初始化完成后再执行测试，避免竞态条件。

## 测试用例覆盖

### 认证测试（`auth.test.js`）— 8 个用例

| 用例 | 说明 | 期望状态码 |
|------|------|-----------|
| 管理员默认凭据登录 | 正常登录流程 | 200 |
| 用户名或密码为空 | 参数校验 | 400 |
| 密码错误 | 安全校验 | 401 |
| 不存在的用户 | 安全校验 | 401 |
| 未登录访问 /me | 权限控制 | 401 |
| 登录后访问 /me | 正常获取用户信息 | 200 |
| 登出后访问 /me | Session 销毁验证 | 401 |

### 照片测试（`photos.test.js`）— 9 个用例

| 用例 | 说明 | 期望状态码 |
|------|------|-----------|
| 未登录访问照片列表 | 权限控制 | 401 |
| 正常上传照片 | 文件上传 + 元数据保存 | 201 |
| 无文件上传 | 参数校验 | 400 |
| 上传非图片文件 | MIME 类型校验 | 400 |
| 查看照片列表 | 正常查询 | 200 |
| 获取单张照片详情 | 正常查询 | 200 |
| 获取不存在的照片 | 异常处理 | 404 |
| 更新照片信息 | 含标签解析验证 | 200 |
| 更新不存在的照片 | 异常处理 | 404 |

### 相册测试（`albums.test.js`）— 8 个用例

| 用例 | 说明 | 期望状态码 |
|------|------|-----------|
| 未登录访问相册列表 | 权限控制 | 401 |
| 正常创建相册 | 正常流程 | 201 |
| 名称为空创建相册 | 参数校验 | 400 |
| 查看相册列表 | 正常查询 | 200 |
| 更新相册信息 | 正常流程 | 200 |
| 更新不存在的相册 | 异常处理 | 404 |
| 删除相册后照片解关联 | 跨资源数据一致性验证 | 200 |
| 删除不存在的相册 | 异常处理 | 404 |

## 新增测试指南

若要为新接口编写测试，请参考以下模板：

```javascript
const request = require('supertest');
const app = require('../src/app');
const { db } = require('../src/db');

beforeAll(async () => {
  await app.ready;
  // 可选：执行登录获取 Cookie
});

afterAll(() => {
  db.close();
});

describe('新接口测试', () => {
  it('应能正常访问', async () => {
    const response = await request(app).get('/api/your-endpoint');
    expect(response.status).toBe(200);
  });
});
```

## 已知限制

- 当前测试直接操作生产数据库文件（`storage/gallery.db`），测试数据会与真实数据共存。建议在 CI/CD 环境中设置 `NODE_ENV=test` 并切换到独立测试数据库，以避免污染生产数据。
